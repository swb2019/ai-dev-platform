apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: ai-dev-platform
  labels:
    app.kubernetes.io/name: ai-dev-platform
    app.kubernetes.io/component: api-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-dev-platform
    app.kubernetes.io/component: api-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
