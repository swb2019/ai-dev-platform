apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: ai-dev-platform
  labels:
    app.kubernetes.io/name: ai-dev-platform
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-dev-platform
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
